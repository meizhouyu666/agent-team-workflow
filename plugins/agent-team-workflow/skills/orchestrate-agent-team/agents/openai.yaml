interface:
  display_name: "Orchestrate Agent Team"
  short_description: "Plan and coordinate parallel coding agents safely"
  default_prompt: "Use $orchestrate-agent-team to plan this task, delegate safe parallel work, integrate it, and wait for independent review."
